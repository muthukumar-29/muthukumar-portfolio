# Copy this to .env.local and fill in your values

# ── Frontend (Vite) ──────────────────────────────
# URL of the contact API (Nodemailer server)
VITE_CONTACT_API_URL=http://localhost:3001/api/contact

# ── Contact Server (server/.env.local or root .env.local) ──
# Your Gmail address
GMAIL_USER=muthukumarm.2903@gmail.com

# Gmail App Password (NOT your regular password)
# Generate at: https://myaccount.google.com/apppasswords
# Steps: Google Account → Security → 2-Step Verification → App passwords
GMAIL_APP_PASSWORD=fgjpxgjbncqiffcv

# Allowed frontend origin (for CORS)
CORS_ORIGIN=http://localhost:5173

# Server port (optional, defaults to 3001)
PORT=3001
